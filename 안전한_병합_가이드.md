# Dev 브랜치 Fetch 시 파일 보호 가이드

## ⚠️ 현재 상태
- 브랜치: `feat/chat`
- 커밋되지 않은 변경사항 있음
- `src/main/java/com/modeon/backend/chat/` 디렉토리 (새로 만든 파일들)

---

## ✅ 안전한 Fetch & Merge 방법

### 1단계: 현재 변경사항 커밋 (필수!)
```bash
# 변경사항 확인
git status

# 새로 만든 chat 디렉토리 추가
git add src/main/java/com/modeon/backend/chat/

# 수정된 파일들 추가
git add build.gradle
git add src/main/java/com/modeon/backend/BackendApplication.java
git add src/main/java/com/modeon/backend/config/SecurityConfig.java
git add src/main/java/com/modeon/backend/entity/WebSocketConfig.java
git add src/main/java/com/modeon/backend/repository/ChatRoomRepository.java
git add src/main/java/com/modeon/backend/service/ChatService.java

# 삭제된 파일들도 커밋에 포함 (패키지 이동으로 인한 삭제)
git add -u

# 커밋
git commit -m "feat: chat 기능 구현 완료"
```

---

### 2단계: 원격 저장소에 푸시 (백업)
```bash
# 현재 브랜치를 원격에 푸시 (백업)
git push origin feat/chat
```

---

### 3단계: Dev 브랜치 Fetch (안전함)
```bash
# Fetch는 로컬 파일을 덮어쓰지 않음! 안전함
git fetch origin dev
```

**중요:** `fetch`는 원격 저장소의 변경사항만 가져오고, 로컬 파일을 건드리지 않습니다.

---

### 4단계: Dev 브랜치 변경사항 확인
```bash
# Dev 브랜치와 현재 브랜치의 차이 확인
git log feat/chat..origin/dev --oneline

# 어떤 파일이 변경되었는지 확인
git diff feat/chat origin/dev --name-only
```

---

### 5단계: Dev 브랜치 병합 (선택사항)
```bash
# Dev 브랜치를 현재 브랜치에 병합
git merge origin/dev
```

**충돌 발생 시:**
- Git이 자동으로 충돌 파일을 표시합니다
- 충돌 파일을 열어서 수동으로 해결
- `git add` 후 `git commit`으로 병합 완료

---

## 🛡️ 파일 보호 전략

### 방법 1: 별도 브랜치에서 작업 (권장)
```bash
# 이미 feat/chat 브랜치에서 작업 중이므로 안전함
# Dev 브랜치를 병합해도 feat/chat 브랜치의 파일은 보호됨
```

### 방법 2: Stash 사용 (임시 저장)
```bash
# 현재 변경사항을 임시 저장
git stash save "작업 중인 chat 기능"

# Dev 브랜치 fetch & merge
git fetch origin dev
git merge origin/dev

# 저장한 변경사항 복원
git stash pop
```

### 방법 3: 백업 브랜치 생성
```bash
# 현재 상태를 백업 브랜치로 저장
git branch backup/feat-chat-$(date +%Y%m%d)

# 이제 안심하고 merge 가능
git fetch origin dev
git merge origin/dev
```

---

## ⚠️ 주의사항

### Fetch는 안전함
- `git fetch`는 로컬 파일을 절대 덮어쓰지 않습니다
- 원격 저장소의 변경사항만 가져옵니다

### Merge 시 주의
- `git merge`를 할 때만 충돌이 발생할 수 있습니다
- 충돌이 발생하면 Git이 자동으로 표시하므로 수동으로 해결하면 됩니다

### Pull은 위험할 수 있음
- `git pull` = `git fetch` + `git merge`
- 충돌이 발생하면 해결해야 합니다

---

## 📋 현재 해야 할 일

1. **먼저 커밋하세요!**
   ```bash
   git add .
   git commit -m "feat: chat 기능 구현 완료"
   ```

2. **원격에 푸시 (백업)**
   ```bash
   git push origin feat/chat
   ```

3. **이제 안심하고 fetch 가능**
   ```bash
   git fetch origin dev
   ```

---

## 🔍 Chat 관련 파일 목록 (보호해야 할 파일들)

### 새로 만든 파일들
- `src/main/java/com/modeon/backend/chat/dto/ChatMessageDto.java`
- `src/main/java/com/modeon/backend/chat/dto/ChatRoomDto.java`
- `src/main/java/com/modeon/backend/chat/entity/ChatRoom.java`
- `src/main/java/com/modeon/backend/chat/entity/ChatMessage.java`
- `src/main/java/com/modeon/backend/chat/repository/ChatMessageRepository.java`
- `src/main/java/com/modeon/backend/chat/repository/ChatRoomRepository.java`
- `src/main/java/com/modeon/backend/chat/service/ChatService.java`
- `src/main/java/com/modeon/backend/controller/ChatController.java`
- `src/main/java/com/modeon/backend/config/ChatRedisConfig.java`

### 수정한 파일들
- `src/main/java/com/modeon/backend/entity/WebSocketConfig.java`
- `src/main/java/com/modeon/backend/config/SecurityConfig.java`
- `src/main/java/com/modeon/backend/security/JwtService.java`

---

## 💡 추천 워크플로우

```bash
# 1. 현재 작업 커밋
git add .
git commit -m "feat: chat 기능 구현"

# 2. 원격에 푸시 (백업)
git push origin feat/chat

# 3. Dev 브랜치 fetch (안전함)
git fetch origin dev

# 4. 변경사항 확인
git log feat/chat..origin/dev --oneline

# 5. 필요시 병합
git merge origin/dev

# 6. 충돌 해결 후 커밋
git add .
git commit -m "merge: dev 브랜치 병합"
```

---

**결론: Fetch는 안전하지만, Merge 전에 반드시 커밋하세요!**

